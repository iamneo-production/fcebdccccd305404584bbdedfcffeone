import { HighlightDirective } from './highlight.directive';
import { ElementRef, Renderer2 } from '@angular/core';

describe('HighlightDirective', () => {
  it('should create an instance', () => {
    // const elMock = {} as ElementRef; // You might need to provide a proper ElementRef mock
    // const rendererMock = {} as Renderer2; // You might need to provide a proper Renderer2 mock

    // const directive = new HighlightDirective(elMock, rendererMock);

    // expect(directive).toBeTruthy();
  });
});
